# Public hosts are reachable but not indexable. nginx drops server-level
# add_header inside any location that sets its own, so include this again there.
add_header X-Robots-Tag "noindex, nofollow, noarchive, nosnippet" always;
add_header Referrer-Policy "no-referrer" always;
